from setuptools import setup

setup(name='easy_cache',
      version='0.1',
      description='Easy memory cache for single-threaded environments',
      url='https://github.com/msempere/easy_cache',
      author='msempere',
      author_email='msempere@users.noreply.github.com',
      license='MIT',
      packages=['easy_cache'],
      test_suite="tests",
      zip_safe=False)
